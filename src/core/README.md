# Core

Platform-independent core helpers belong here.

The current baseline keeps core support minimal and focuses on:

- public framework APIs
- the device registry skeleton
- the OSAL/runtime boundary
- the Phase-3 lifecycle entry point in `mn_system_init()`
